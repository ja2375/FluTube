name: flutube
description: Flutter plugin that facilitates the embed of YT videos without using the official YT API.
version: 0.8.3
homepage: https://github.com/ja2375/FluTube

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"
  flutter: ">=1.12.13 <2.0.0"

dependencies:
  http: ">=0.12.0 <0.13.0"
  open_iconic_flutter: ">=0.3.0 <0.4.0"
  cached_network_image: ^2.0.0-rc.1
  flutter_android_pip: ^0.0.2
  video_player: ">=0.10.5 <0.11.0"
  flutter:
    sdk: flutter

flutter:
